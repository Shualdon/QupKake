name: qupkake
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python=3.10
  - pip
  - pytorch-cuda=12.2
  - pytorch::pytorch==2.1
  - torchmetrics 
  - torchvision
  - cudatoolkit
  - pytorch-lightning=2.0.2 
  # - pyg::pyg==2.3
  # - pytorch-scatter::pyg
  # - pytorch-sparse::pyg
  - pandas
  - ipython
  - jupyter
  - ipykernel
  - rdkit=2022.03.03
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - torch-geometric==2.3
    - torch-sparse
    - torch-scatter
  #  - codecov

