name: qupkake
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pytorch::pytorch==2.1
  - torchmetrics 
  - torchvision
  - cudatoolkit
  - pytorch-lightning=2.1.0
  # - pyg=2.4
  - pandas
  - numpy
  - scipy
  - rdkit=2022.03.03
    # Testing
  - pytest
  - pytest-cov
  #- codecov

    # Pip-only installs
  - pip:
    - torch_geometric==2.4
    # - torch-sparse
    # - torch-scatter
    - codecov

