name: qupkake
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - pip
  - pytorch::pytorch==2.1
  - torchmetrics 
  - torchvision
  - cudatoolkit
  - pytorch-lightning=2.1.0
  - pyg::pyg==2.3
  - pytorch-scatter::pyg
  - pytorch-sparse::pyg
  - pandas
  - ipython
  - jupyter
  - ipykernel
  - rdkit=2022.03.03
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  # - pip:
    # - torch_geometric==2.3
    # - torch-sparse
    # - torch-scatter
    # - codecov

